<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:TP5.ViewModels"
             x:Class="TP5.Views.AuthentificationPage"
             Title="Authentification">

    <ContentPage.BindingContext>
        <viewmodels:AuthentificationViewModel></viewmodels:AuthentificationViewModel>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout Padding="10">
            <Label Text="Your login :"></Label>
            <Entry Text="{Binding UserLogin}"></Entry>
            <Label Text="Your password :"></Label>
            <Entry Text="{Binding Password}" IsPassword="True" IsEnabled="True"></Entry>

            <StackLayout Orientation="Horizontal">
                <Button Text="Login" 
                        Command="{Binding CmdLogin}" 
                        HorizontalOptions="Center"></Button>
                <Button Text="Clear" 
                        Command="{Binding CmdDelete}"
                        HorizontalOptions="Center"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>